spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/ecommerce_m_v?autoReconnect=true
    username: root
    password: Learn_ubuntu1
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
    active: 'dev'